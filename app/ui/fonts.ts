import { Inter, Lusitana, Oswald } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })
export const lusitana = Lusitana({
  weight: '400',
  subsets: ['latin']
})
export const oswald = Oswald({ weight: 'variable', subsets: ['latin'] })